<ul class="list-group list-group-numbered">
  <% @lists.each do |list| %>
    <%= link_to list_path(list) do %>
      <li class="list-group-item btn btn-primary mt-3"><%= list.name %></li>
    <% end %>
  <% end %>
</ul>

<%= link_to "New", new_list_path, class: "btn btn-success mt-3"%>
